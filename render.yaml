services:
  - type: web
    name: shadow-ascension-lan
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run server
    healthCheckPath: /api/status
    envVars:
      - key: NODE_VERSION
        value: 22
