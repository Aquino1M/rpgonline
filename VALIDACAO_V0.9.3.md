# Validação V0.9.3

- Sintaxe JavaScript verificada com `node --check` em `src/game`, `server`, `api`, `tools` e `scripts`: OK.
- Arquivo principal importa `gameplayV093.js`.
- Versão visível atualizada para V0.9.3.
- O pacote final deve ser testado com `JOGAR_LOCAL.bat` em um ambiente com Node.js/NPM e dependências instaladas.
- No ambiente de empacotamento, a instalação das dependências NPM excedeu o tempo disponível; por isso o build Vite completo não foi executado aqui.
